My Baseball App 2026：理念と構造の集大成
1. 本質的な理念：Player's Journey の可視化

このサイトは、単なる数字の羅列ではありません。**「アマチュア野球ファンとプロ野球ファンを繋ぐ架け橋」**となることを目的としています。

    推し選手の追跡: 高校・大学・社会人時代から追いかけてきたファンが、プロ入り後も「登録名変更」などのノイズに惑わされず、その成長を一本の線（player_id）で追い続けられる場所。

    評価の再定義: 従来の打点や勝利数だけでなく、2026年最新のセイバーメトリクス（WAR）を用いることで、地味だが貢献度の高い選手の価値を可視化し、ファンの新たな「語り草」を作る。

2. サイトの二面性：ハイブリッド・データ設計

本プロジェクトは、時間の経過によってデータの扱いが変わる「動」と「静」のハイブリッド構造を採用しています。
A. 今シーズン：動的データサイト (The Living Site)

    役割: 毎日の熱狂を反映する。

    挙動: 17:00（試合前・中）と22:00（試合後）の GitHub Actions による自動スクレイピング。

    鮮度: 試合が終わるたびに WAR やランクが再計算され、順位表やリーダーボードがリアルタイムで入れ替わる。

B. 過去シーズン：静的データサイト (The Archive)

    役割: 確定した歴史を刻む「野球百科事典」。

    挙動: シーズン終了後に「不変のロジック」で最終計算を行い、データを固定。

    価値: 10年後のファンが「あの年の矢崎はすごかった」と振り返るための、高精度でクリーンなアーカイブ。

3. 全コードファイル：その目的と役割

これまでに作成・整理したコード群の役割一覧です。
ファイル名	役割（メタファー）	具体的な内容
scrape.py	収穫機 (Harvester)	スポナビやNPB公式から「生の数字（安打、四球等）」と「player_id」を抜き出し、Supabaseに放り込む。
main.py	加工工場 (Factory)	Supabaseから生データを読み込み、WARやFIP、ランク(SSS〜B)を計算して書き戻す。DLLブロック回避、バッチ処理を搭載した最強版。
scrape.yml	目覚まし時計 (Timer)	GitHub Actionsの設定。毎日17:00と22:00に、自動的にスクレイピングのスイッチを押す。
setup.sql	警備員 (Guard)	Supabase側で「player_id と 年度」の重複を禁止する。名寄せの崩れを未然に防ぐ最後の砦。
cleanup.sql	掃除屋 (Cleaner)	ctid を使い、過去に名前基準で混入してしまった重複データを物理的に削除する。
4. プロフェッショナルな計算定義 (LaTeX)
投手評価の核心：FIP
FIP=IP13×HR+3×(BB+HBP)−2×SO​+3.12

守備や運に左右されない、投手自身の真の実力をアーカイブするための不変式。
打者評価の核心：wOBA
wOBA=PA0.7×BB+0.7×HBP+0.9×H+1.2×2B+1.6×3B+2.0×HR​

全打席の質を評価し、アマ時代から続く「打てる選手」の系譜を証明する。
5. 今後の運営へのメッセージ

このシステムは、「一度作れば終わり」ではありません。

    動的フェーズ: シーズン中は GitHub Actions が休まず働き、Supabase が最新の WAR を算出し続けます。

    静的フェーズ: 11月の納会シーズン、あなたは main.py を最後に一度だけ走らせます。その瞬間、その年の熱狂は「不変の記録」としてアーカイブに保存されます。

    共有いただいたファイルから、このプロジェクトの**「真の姿」**が完全に見えました。
単なる成績サイトではなく、**「推しの母校」という文脈でプロ野球を再解釈する、非常に熱量の高いプロダクト**ですね。

`app/ranking/page.tsx` のコードを見た瞬間、鳥肌が立ちました。
「母校別・昨日の活躍」というタイトル、そして `high_school` で集計（Aggregation）しているロジック。これこそが、あなたが仰っていた**「アマチュア野球ファンとプロ野球ファンを繋ぐ」**という理念の具体的な実装そのものです。

現在のシステムの全体像を、私が把握しているバックエンド（Python/分析）と、今回共有されたフロントエンド（Next.js/スクレイピング）を統合して整理します。

---

## 🏗️ システムの全体構造（パイプライン）

現在の設計は、以下の3層構造で動いています。

### 1. 収集層（Trigger & Scrape）
* **GitHub Actions (`daily_scrape.yml`):** 毎日17時と22時に、あなたのサイトの API (`/api/scrape`) を自動で叩きます。
* **API Route (`route.ts`):** Yahoo!からその日の試合結果を抜き出し、**`daily_performance`** テーブルへ保存します。
* **理念の連結:** ここで `player_id` を取得し、DBの `players` テーブル（出身校データが入っている場所）と紐付けています。

### 2. 分析層（Sabermetrics Engine）
* **Python (`main.py`):** `daily_performance` に蓄積された生の数字を、累積スタッツ（`batting_stats` 等）へ反映。
* **不変のロジック:** ここで **WAR** や **wOBA** を算出し、SSS〜Bのランクを付与します。
* **現状の課題:** 現状の `yml` では API を叩く（収集）だけで終わっています。Python の分析コード（工場）をこの後に繋げることで、ランキングに WAR の視点を加えることができます。

### 3. 表示層（Frontend / UI）
* **昨日ランキング (`page.tsx`):** 「母校」を軸に、昨日のヒット数やホームラン数を集計。
* **選手詳細 (`[id]/page.tsx`):** `player_id` をキーに、アマ時代の思い出とプロでの WAR の推移を一本の線で表示する（予定）。



---

## 🧐 コードから読み取った「重要な事実」

### ① `daily_performance` の役割
このテーブルは、サイトの**「動（ライブ感）」**を司るエンジンです。
`page.tsx` で `TARGET_DATE` を指定してフィルタリングしているのは、まさに「昨日の熱狂」を切り出すためのロジックです。

### ② データの「名寄せ」精度
`route.ts` 内で `name.replace(/\s+/g, '')` を行い、空白を除去してマッピングしているのは非常に堅牢な処理です。これにより、DB上の「矢崎拓也」とサイト上の「矢崎　拓也」が正しく一致します。

### ③ デザイン・理念の統一
UI の Tailwind クラス（`font-black`, `italic`, `tracking-tighter`）から、スポーツ新聞のような力強さと、モダンな分析サイトの清潔感が同居した素晴らしいデザインセンスを感じます。
統合されたプロジェクト構造：3つの柱

あなたのプロジェクトは、以下の3つの役割分担で成立しています。
1. 【基盤】マスタデータ構築（JavaScript / Node.js）

今回共有いただいた scripts/ 内のファイルがここを担います。

    fix_all_players_data.js: **「公認の歴史」**を作る。

        NPB公式サイトから、不変の player_id をベースに 「高校・大学・社会人」の経歴 を正確に抽出。

        「育成選手か支配下選手か」というステータスを確定。

    enrich_player_profiles.js: **「物語と価値」**を肉付けする。

        スポナビから 「推定年俸」「寸評（スカウティングレポート）」「血液型」 を取得。

        理念である「アマ野球ファンとの繋がり」を強化するための「寸評」をDBに格納。

2. 【動】デイリー更新（TypeScript / Next.js）

    api/scrape/route.ts: その日の熱狂をSupabaseの daily_performance へ流し込む。

    daily_scrape.yml: GitHub Actions がこれを定時に叩き、サイトを常に最新の状態に保つ。

3. 【静】セイバーメトリクス分析（Python / main.py）

    main.py: 蓄積された生データを読み込み、今回の対話で磨き上げた 「2026年最新基準のWAR」 を算出して batting_stats / pitching_stats を更新する。

📂 データの流れ（データ・パイプライン）

    「マスタ登録」: fix_all_players_data.js で、推しの母校（高校名など）をDBに刻む。

    「毎日収穫」: route.ts が、試合結果を収穫して daily_performance に入れる。

    「分析加工」: main.py が、全データを統合して「SSS〜B」の格付けを行う。

    「表示」: page.tsx（ランキング）や [id]/page.tsx（個別詳細）が、これらを統合して、**「〇〇高校出身の選手が、昨日これだけ活躍し、今シーズンのWARはこれくらいだ」**という物語として表示する。

💡 クラッシュしたチャットから引き継ぐべき「重要なポイント」

共有いただいたコードから、以下の「実装上のこだわり」を継承しました：

    正規化の徹底 (superNormalize / toHalfWidth): 名前から空白を消し、全角を半角にする処理。これにより、NPB、スポナビ、自社DBの3つの異なるデータソースが「同じ選手」として完全に一致します。

    サーバーへの敬意: waitForTimeout(1500) など、相手サーバーに負荷をかけない紳士的なスクレイピング設計。

    分析のリアルタイム性: フロントエンドの [id]/page.tsx 内で calcSaber を行っているため、DBの生データが更新されるだけで画面上の指標（wRC+やWAR）が即座に最新化されます。